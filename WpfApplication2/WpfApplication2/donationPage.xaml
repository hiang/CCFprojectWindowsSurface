<Page x:Class="WpfApplication1.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Page1">

    <Grid>
        <!-- <ListBox ItemsSource="{Binding Source={StaticResource MyPhotos}}"
         Background="Silver" Width="600" Margin="10" SelectedIndex="0"/> -->

        <Grid x:Name="inputBox">
            <Border
            MinWidth="300"
            BorderThickness="1"
            CornerRadius="0,60,50,0"
            Padding="2"
            HorizontalAlignment="Left">
                <Label>Another field</Label>
            </Border>

            <!-- Textfield for the email address-->
            <TextBox Margin="90,6,157,86">Another value</TextBox>

            <!--align the background edges to the left -->
            <Image Height="100" Width="100" Source="qrcode.png" Margin="120,102,80,98" />

        </Grid>


    </Grid>
</Page>
